const Store = require('../client/Store');
const mockData = require('../../db.json');

describe('empty test', () => {
	it('should do nothing', () => {
		// Arrange

		// Act

		// Assert
		expect(1).toEqual(1);
	});
});
