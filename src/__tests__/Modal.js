const Modal = require('../client/Modal');
const Store = require('../client/Store');
const mockData = require('../../db.json');

// GIVEN I am on the search page
// WHEN I click on a film
// THEN a modal appears, displaying the poster, title, year, plot, director, genre and cast of the selected film
describe('openModal', () => {
	it('should open a modal for the correct film', () => {
		
		// Arrange
		// Act
		// Assert
	});
}); 
